# Minimum to run stub_sim.py / stub_brain.py / stub_panel.py / contract.py on either machine.
# No torch, no ultralytics. Python >= 3.12 (numpy 2.5.x needs 3.12+; websockets 17 needs 3.11+).
opencv-python==4.14.0.94
numpy==2.5.3
pyyaml==6.0.3
websockets==17.1
